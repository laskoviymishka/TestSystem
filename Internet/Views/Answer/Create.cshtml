@model Internet.Models.Answer

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Answer</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.AnswerBody)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AnswerBody)
            @Html.ValidationMessageFor(model => model.AnswerBody)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IsRight)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IsRight)
            @Html.ValidationMessageFor(model => model.IsRight)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
